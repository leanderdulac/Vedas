{$DOMAIN:localhost} {
    encode gzip zstd

    # Métricas restritas para rede interna ou autorização se necessário
    handle /metrics {
        reverse_proxy api:8000
    }

    handle /api/* {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy api:8000
    }
}
